pub fn validate_tag(tag: String) -> Result<(), bool> {
     /// tag.lenght should >= 3
     /// tag should be just letter
     /// tag can not contain numbers
     /// tag can not contain special characters
    Ok(true)
}